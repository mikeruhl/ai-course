[project]
name = "module-02-prompt-engineering"
version = "0.1.0"
description = "Lab: Prompt Engineering at Staff Level"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "tiktoken>=0.7",
    "python-dotenv>=1.0",
    "rich>=13.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]

[project.scripts]
harness = "harness:main"
