[project]
name = "module-05-structured-outputs"
version = "0.1.0"
description = "Lab: Structured Outputs at Depth — Pydantic schemas, strict mode, extraction pipelines"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "rich>=13.0",
    "pydantic>=2.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
