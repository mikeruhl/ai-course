[project]
name = "module-03-tool-use"
version = "0.1.0"
description = "Lab: Function Calling / Tool Use — the raw agent loop"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "rich>=13.0",
    "google-auth>=2.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
