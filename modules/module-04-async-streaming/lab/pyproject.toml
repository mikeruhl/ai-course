[project]
name = "module-04-async-streaming"
version = "0.1.0"
description = "Lab: Async Agent Loops + Streaming — concurrent tool execution and SSE"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "rich>=13.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]
