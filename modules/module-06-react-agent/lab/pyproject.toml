[project]
name = "module-06-react-agent"
version = "0.1.0"
description = "Lab: ReAct Agent from Scratch"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "rich>=13.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
